<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.business.mapper.CBalanceLogMapper" >
  <resultMap id="BaseResultMap" type="com.ruoyi.business.model.CBalanceLog" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="balance_id" property="balanceId" jdbcType="BIGINT" />
    <result column="uid" property="uid" jdbcType="BIGINT" />
    <result column="before_amount" property="beforeAmount" jdbcType="DECIMAL" />
    <result column="after_amount" property="afterAmount" jdbcType="DECIMAL" />
    <result column="amount" property="amount" jdbcType="DECIMAL" />
    <result column="remarks" property="remarks" jdbcType="VARCHAR" />
    <result column="type" property="type" jdbcType="VARCHAR" />
    <result column="fid" property="fid" jdbcType="BIGINT" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
  </resultMap>

  <select id="getBackBalanceLogList" resultType="com.alibaba.fastjson2.JSONObject">
    select t2.address,t2.currency,t1.amount,t1.remarks,t1.create_time createTime,t2.balance_type balanceType,
    t1.before_amount beforeAmount,t1.after_amount afterAmount
    from c_balance_log t1
    left join c_balance t2 on t1.balance_id=t2.id
    where 1=1 and t1.amount>0
    <if test="coin !=null and coin !=''">
      and   t2.currency=#{coin}
    </if>

    <if test="address !=null and address !=''">
      and t2.address=lower(#{address})
    </if>
    <if test="startTime !=null">
      and to_days(t1.create_time) >= to_days(#{startTime})
    </if>
    <if test="endTime !=null">
      and to_days(t1.create_time) &lt;= to_days(#{endTime})
    </if>
    <if test="balanceType !=null and balanceType !=''">
      and t2.balance_type=#{balanceType}
    </if>
    order by t1.id desc
  </select>
</mapper>